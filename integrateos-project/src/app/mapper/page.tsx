import { MappingStudio } from "@/components/mapper/MappingStudio";

export const metadata = {
  title: "Mapping Studio — IntegrateOS",
};

export default function MapperPage() {
  return <MappingStudio />;
}
